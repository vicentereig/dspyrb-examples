---
title: "<%= month %> <%= year %>"
---

import { PrList } from "/snippets/pr-list.mdx";

<% themes.each do |theme| %>
## <%= theme.name %>

<%= theme.description %>

<% theme.subthemes.each do |subtheme| %>
### <%= subtheme.title %>

<%= subtheme.description %>

<% end %>
<PrList>
  {[<%= theme.pr_ids.join(', ') %>]}
</PrList>

<% end %>